I have created two python files for this Cryptography unit. 
The first is a python program which encrytps a simple string the second
can take an external file in this case a csv and output an encrypted version as a file (the class csv).

